//for flickr keys, but I am not sure we even need this. the method i used to call the photos back did not use the keys?
exports.flickr = {
  id: process.env.flickr_ID,
  secret: process.env.secret_ID
};
